export const initialState = {
    users: [],
};

export const usersReducer = (state = initialState, action) => {
    switch (action.type) {
        // TODO
        default:
            return state;
    }
};
